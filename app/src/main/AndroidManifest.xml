<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> <!-- Android 13+ -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <application
        android:name=".Der3Application"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/der3_muslim_title"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Der3Muslim">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/der3_muslim_title"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <!--  خدمة استقبال التنبيهات (Firebase Cloud Messaging)
         تسمح هذه الخدمة للتطبيق باستقبال الرسائل والتنبيهات من سيرفر Firebase حتى لو كان التطبيق مغلقاً.
         -->
        <service
            android:name="com.der3.shared.service.fcm.Der3FirebaseMessagingService"
            android:directBootAware="true"
            android:exported="false">
            <intent-filter>
                <!-- استقبال الرسائل المرسلة من خوادم Google (المعيار القديم والجديد) -->
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
                <!-- الحدث الرئيسي لمعالجة الرسائل الجديدة وتحديث الـ Token الخاص بالجهاز -->
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
                <!-- الحدث الرئيسي لمعالجة الرسائل الجديدة وتحديث الـ Token الخاص بالجهاز -->
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>